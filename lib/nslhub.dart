import 'package:flutter/material.dart';

class Nslhub extends StatelessWidget {
  const Nslhub({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('nslhub'),
    );
  }
}